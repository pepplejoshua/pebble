type Box[T] = struct {
    value T;
    fn replace[U](self Box[T], left, right U) U => left;
};

type Plain = struct {
    fn get(self Plain) i32 => 1;
};

fn choose(first, second i32) i32 => first;

extern {
    fn foreign(value i32) i32;
    let external i32;
}
