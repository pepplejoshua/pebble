type Point = struct {
    x int;
    y int;
};

fn f() void {
    var arr [3]int = [1, 2, 3];
    let s []int = arr[:];
    print s;
    var points [2]Point = [Point.{ x = 1, y = 2 }, Point.{ x = 3, y = 4 }];
    let ps []Point = points[:];
    print ps;
}
