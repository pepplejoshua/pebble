# Type inference fixtures

These source-driven cases exercise the type-syntax portion owned by compiler
phase 05b. Solver identity, equations, rollback, and exact literals use direct
Go assertions in `compiler/internal/infer`.
