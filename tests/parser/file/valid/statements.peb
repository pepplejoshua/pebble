fn statements() int {
    var total int = 0;
    let values = [1, 2, 3];
    {
        var nested = 1;
    }
    if total == 0 {
        total = 1;
    } else {
        total += 2;
    }
    while total < 4 {
        total++;
    }
    loop 0..10: index {
        if index == 5 break;
        continue;
    }
    for var index = 0; index < 3; index += 1 {
        print index, values[index];
    }
    for ;; total++;
    switch total {
        case 0, 1: { total = 2; }
        else: { total = 3; }
    }
    defer { print total; }
    return total;
}
