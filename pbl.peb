extern fn system(cmd str) int;
extern fn strlen(s str) usize;

fn main() int {
    var greeting = "hi";
    print strlen(greeting);

    // show folder contents
    system("ls -l");


    return 0;
}
