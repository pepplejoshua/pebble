fn deref(a *int) void {
    if a == nil {
        print "ptr to nil";
    } else {
        print *a;
    }
}

fn main() int {
    var a *int = nil;
    var b = 5;
    deref(a);
    deref(&b);
    return 0;
}
