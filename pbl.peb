type Node = struct {
    data int,
    next *Node
};

type TNode = (int, *TNode);

type Point = (int, int);
type Vec2 = (int, int);

fn point_2_vec2(p Point) Vec2 => p

fn main() int => point_2_vec2((0, 1)).0
