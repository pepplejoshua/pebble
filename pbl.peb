type Header = struct {
    type_of int;
};

type Foo = struct {
    header Header;
    value int;
    l []char;
};

type Bar = struct {
    header Header;
    value float;
};

fn main() int {
  return 0;
}
