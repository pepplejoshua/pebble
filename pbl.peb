type Node = struct {
    data int;
    next *Node;
};

type Pair = (int, *Pair);

type IntNode = (int, *IntNode);

fn main() int {
    var n = Node.{
        data = 0,
        next = nil,
    };

    return 0;
}
