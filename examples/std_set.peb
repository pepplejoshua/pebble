import "std:hash";
import "std:set";

fn main() int {
    let strings = [
        "123",
        "456",
        "789"
    ];

    var s = set::new.[str](hash::hash_str, fn (a, b str) bool => a == b);
    defer set::delete(&s);

    loop 0..strings.len : i {
        s.insert(strings[i]);
    }

    var x = "123";

    print `set contains 123? {s.contains("123")}`;
    print `set contains 345? {s.contains("345")}`;

    return 0;
}
