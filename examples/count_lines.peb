import "std:io";
import "std:string";
import "std:hmap";

type Stats = struct {
    lines uint;
    words uint;
    chars uint;
};

// Fills *stats with the line/word/char counts for filename. Returns false if
// the file could not be opened.
fn count_file(filename str, stats *Stats) bool {
    var file = io::open(filename, io::MODE_READ);
    if file == nil {
        return false;
    }

    var contents = io::read_all(file);
    io::close(file);

    stats.lines = 0;
    stats.words = 0;
    stats.chars = contents.len;

    // Count newline bytes. String.data is raw UTF-8 bytes, so byte-level
    // ASCII detection is correct here (no decoding needed).
    var i = 0;
    while i < contents.len {
        if contents.data[i] == ('\n' as u8) {
            stats.lines++;
        }
        i++;
    }

    // Add 1 to line count if file doesn't end with newline and has content
    if contents.len > 0 {
        var last_char = contents.data[contents.len - 1];
        if last_char != ('\n' as u8) {
            stats.lines++;
        }
    }

    // Count words
    var in_word = false;
    var j = 0;
    while j < contents.len {
        var c = contents.data[j];
        if c == (' ' as u8) || c == ('\t' as u8) || c == ('\n' as u8) || c == ('\r' as u8) {
            in_word = false;
        } else {
            if !in_word {
                stats.words++;
                in_word = true;
            }
        }
        j++;
    }

    string::delete(&contents);
    return true;
}

fn main() int {
    var total_stats Stats = Stats.{ lines = 0, words = 0, chars = 0 };
    var successful_files = 0;
    var paths = [
        "compiler/std/func.peb",
        "compiler/std/hash.peb",
        "compiler/std/hmap.peb",
        "compiler/std/io.peb",
        "compiler/std/libc.peb",
        "compiler/std/mem.peb",
        "compiler/std/set.peb",
        "compiler/std/string.peb",
        "compiler/std/vec.peb",
        "compiler/std/mem/arena.peb",
    ];

    // Count and report each file.
    loop 0..paths.len : index {
        var filename str = paths[index];
        var stats Stats = Stats.{ lines = 0, words = 0, chars = 0 };
        if count_file(filename, &stats) {
            total_stats.lines += stats.lines;
            total_stats.words += stats.words;
            total_stats.chars += stats.chars;
            successful_files++;
            println filename, ": ", stats.lines, " lines, ", stats.words, " words, ", stats.chars, " chars";
        } else {
            println "Error reading ", filename;
        }
    }

    println "";
    println "Summary for ", successful_files, "/", paths.len, " files:";
    println "Total lines: ", total_stats.lines;
    println "Total words: ", total_stats.words;
    println "Total chars: ", total_stats.chars;

    return 0;
}
